
#
NEXT_PUBLIC_INTERSTITIAL_AD_ID=ait.live.c3342a482ba9472e

NEXT_PUBLIC_ENVIRONMENT=production

# Optional: share link when running outside the Toss app (defaults to GitHub repo).
# NEXT_PUBLIC_WEB_SHARE_URL=https://example.com/your-live-app

# Backend (Grab API proxy) — defaults to http://localhost:4000 if unset.
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000
